development:
  adapter: async

test:
  adapter: test

production:
  adapter: postgres
  # adapter: redis
  # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  # channel_prefix: bbq_bud_production
